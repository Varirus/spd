#ifndef ALGORITHMS_H
#define ALGORITHMS_H

#include <vector>
#include "Structs.h"
#include <limits.h>

//std::pair<Result, Result> bruteForce(std::vector<Job> jobs);
Result lsa(int m, std::vector<Job> original_jobs);

#endif